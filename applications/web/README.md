# Safe Guard Web

Next.js monitoring console for the clean rebuild.

Install and run:

```powershell
npm install
npm run dev
```

Useful checks:

```powershell
npm run typecheck
npm run build
```

The app reads `NEXT_PUBLIC_API_BASE_URL` and defaults to `http://localhost:8000`.
