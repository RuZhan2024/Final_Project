# Claim Table

Dataset role policy for this table:
- Primary deployment target dataset: `CAUCAFall`
- Comparative dataset: `LE2i` (used for comparison/generalization evidence, not primary deployment acceptance)

| Claim ID | Claim Statement | Metric(s) | Threshold | Dataset(s) | Protocol | Evidence Artifact(s) | Reproduce Command | Failure Condition | Pass/Fail Rule | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| C1 | Promoted TCN profile on CAUCAFall reaches zero false alerts at OP2 while retaining high recall. | `FA24h`, `Recall`, `F1`, `AP` | `FA24h = 0` and `Recall >= 0.80` | `CAUCAFall (test, primary)` | Fit OP on val, evaluate on test (`selected=op2`). | `outputs/metrics/tcn_caucafall_cauc_hneg1_confirm0.json`, `configs/ops/tcn_caucafall_cauc_hneg1_confirm0.yaml` | `python scripts/eval_metrics.py --win_dir data/processed/caucafall/windows_eval_W48_S12/test --ckpt outputs/caucafall_tcn_W48S12_cauc_hneg1/best.pt --ops_yaml configs/ops/tcn_caucafall_cauc_hneg1_confirm0.yaml --out_json outputs/metrics/tcn_caucafall_cauc_hneg1_confirm0.json` | `FA24h > 0` or `Recall < 0.80` | Pass only if both conditions are met. | **PASS** (`FA24h=0.0`, `Recall=0.80`, `F1=0.8889`, `AP=0.9819`) |
| C2 | Promoted comparative GCN profile on CAUCAFall also achieves zero false alerts with matching event F1/Recall, but lower AP than TCN. | `FA24h`, `Recall`, `F1`, `AP` | `FA24h = 0` and `Recall >= 0.80` | `CAUCAFall (test, comparative arch)` | Fit OP on val, evaluate on test (`selected=op2`). | `outputs/metrics/gcn_caucafall.json`, `configs/ops/gcn_caucafall.yaml` | `python scripts/eval_metrics.py --win_dir data/processed/caucafall/windows_eval_W48_S12/test --ckpt outputs/caucafall_gcn_W48S12/best.pt --ops_yaml configs/ops/gcn_caucafall.yaml --out_json outputs/metrics/gcn_caucafall.json` | `FA24h > 0` or `Recall < 0.80` | Pass only if both conditions are met. | **PASS** (`FA24h=0.0`, `Recall=0.80`, `F1=0.8889`, `AP=0.9640`) |
| C3 | LE2i serves as a comparative benchmark where both architectures remain usable under current OP2 policy. | `AP`, `F1` | For each model: `AP >= 0.75` and `F1 >= 0.75` | `LE2i (test, comparative)` | Fit OP on val, evaluate on test (`selected=op2`). | `outputs/metrics/tcn_le2i.json`, `outputs/metrics/gcn_le2i.json` | `python scripts/eval_metrics.py --win_dir data/processed/le2i/windows_eval_W48_S12/test --ckpt outputs/le2i_tcn_W48S12/best.pt --ops_yaml configs/ops/tcn_le2i.yaml --out_json outputs/metrics/tcn_le2i.json && python scripts/eval_metrics.py --win_dir data/processed/le2i/windows_eval_W48_S12/test --ckpt outputs/le2i_gcn_W48S12/best.pt --ops_yaml configs/ops/gcn_le2i.yaml --out_json outputs/metrics/gcn_le2i.json` | Any model falls below AP or F1 threshold | Pass only if both models meet both thresholds. | **PASS** (TCN: `AP=0.8541`, `F1=0.8235`; GCN: `AP=0.7523`, `F1=0.7500`) |
| C4 | On comparative LE2i OP2 results, TCN currently outperforms GCN in event F1. | `F1 delta` | `F1(TCN)-F1(GCN) >= 0.05` | `LE2i (test, comparative)` | Same split, same eval protocol, separate ckpt+ops. | `outputs/metrics/tcn_le2i.json`, `outputs/metrics/gcn_le2i.json` | `python scripts/summarize_metrics_table.py` *(or compute from the two metrics JSON files directly)* | `delta < 0.05` | Pass only if delta threshold is met. | **PASS** (`0.8235 - 0.7500 = 0.0735`) |

## Notes
- All values above are read from current repository artifacts.
- Current FA24h on LE2i is high (`581.58`) for both models; this is reported as comparative evidence, not as primary deployment acceptance criteria.
- Multi-seed stability artifacts are now available; integrate CI summaries from `artifacts/reports/stability_summary.csv` into final thesis tables.
- Unlabeled LE2i results (e.g., `artifacts/reports/hneg_cycle/*unlabeled_fa.json`) are treated as open-world stress indicators only. If unlabeled clips may contain falls, `fa24h` from those files is not a strict false-alarm claim.
